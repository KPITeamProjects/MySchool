<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%=title%></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<header>
    <%- include ('templates/header.ejs');-%>
</header>
<body>
<div class="body">
    <div class="inherit-body">
        <table id="to-do">
            <tr id="head_eve">
                <th>Необхідно виконати</th>
            </tr>
            <%
            if(userData.length!=0){
                var i=1;
            userData.forEach(function(data){
            %>
            <tr>
                <td><%=i+") "+lesson_name+": "+task+" "+ deadline%></td>
            </tr>
            <%  i++; })} %>
        </table>
    </div>
</div>
<div class="body">
    <div class="inherit-body">
        <table id="to-do2">
            <tr id="head_eve2">
                <th>Виконано</th>
                <th></th>
            </tr>
            <%
            if(userdoneData.length!=0){
                var ii=1;
            userdoneData.forEach(function(data){
            %>
            <tr>
                <td><%=lesson_name+": "+task+" "+ deadline%></td>
            </tr>
            <%  ii++; })} %>
        </table>
    </div>
</div>
</body>
</html>